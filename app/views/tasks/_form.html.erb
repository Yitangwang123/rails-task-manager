<%= form_for(task) do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title %>
  <%= t.label :details %>
  <%= t.text_field :details %>
  <% if task.persisted? %>
    <%= t.label :completed %>
    <%= t.text_field :completed %>
  <% end %>
  <%= t.submit %>
  <% end %>
